/**
 * The two documents that STILL have a live copy on the Framer marketing site.
 *
 * emergences.ai serves its own Website Privacy Notice and Terms of Use from
 * Framer. Until those pages are retired or redirected here, each of these
 * documents is published at two public addresses — the exact drift this whole
 * arrangement exists to end. The product's copies were kept out of the index
 * for the same reason before they were redirected away.
 *
 * So they are noindexed HERE too, and left out of the sitemap: whichever copy a
 * search engine finds, there is only one. They are still linked from the index
 * and still reachable, because a reader who follows a link should get the
 * document — it is discovery that has to be single, not access.
 *
 * REMOVE THIS FILE and its two call sites in the same change that retires the
 * Framer pages. It is a bridge, not a policy.
 */
export const PENDING_FRAMER_RETIREMENT = new Set([
  "website-privacy",
  "website-terms",
]);
